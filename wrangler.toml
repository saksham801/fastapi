name = "fastapi"
main = "main.py"
compatibility_date = "2026-03-30"
compatibility_flags = ["python_workers"]

# Use this specific section format to ensure the bundler wakes up
[vars]
PYTHON_VERSION = "3.11"

[python]
# Keep this empty but ensure no trailing spaces or hidden characters
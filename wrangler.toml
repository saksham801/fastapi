name = "fastapi"
main = "main.py"
compatibility_date = "2026-03-30"
compatibility_flags = ["python_workers"]

[python]
# Drop the [python] block if it keeps causing warnings.
# The compatibility_flag is what actually triggers the engine.
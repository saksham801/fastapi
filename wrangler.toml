name = "fastapi"
main = "main.py"
compatibility_date = "2026-03-30"
compatibility_flags = ["python_workers"]

# This is likely where the error is.
# Ensure there are NO spaces or tabs inside this section.
[python]